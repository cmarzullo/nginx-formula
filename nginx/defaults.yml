# -*- coding: utf-8 -*-
# vim: ft=yaml
# Defaults for nginx
nginx:
  enabled: False
  required_pkgs:
    - nginx
  conf_source: 'salt://nginx/files/nginx.conf.j2'
  service:
    name: nginx
    state: dead
    enable: False
    user: www-data
